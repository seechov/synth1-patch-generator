[package]
name = "synth1gan"
version = "1.0.0"
edition = "2021"
authors = ["Aleksei Sychev <seechov@protonmail.com>"]
description = "Synth1 VST preset generator powered by a WGAN-GP neural network"
license = "MIT"

[[bin]]
name = "synth1gan"
path = "src/main.rs"

[dependencies]
eframe = { version = "0.29", default-features = false, features = [
    "default_fonts",
    "glow",
    "persistence",
] }
egui = "0.29"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# Pure-Rust ONNX runtime — no external DLLs needed, works on Windows + Linux
tract-onnx = "0.21"
rfd = "0.14"
rand = "0.8"
rand_distr = "0.4"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true

[target.'cfg(windows)'.build-dependencies]
winres = "0.1"

[package.metadata.deb]
maintainer = "Aleksei Sychev <seechov@protonmail.com>"
section = "sound"
priority = "optional"
depends = "$auto"
assets = [
  ["target/release/synth1gan", "usr/bin/synth1gan", "755"],
  ["assets/synth1gan.desktop", "usr/share/applications/synth1gan.desktop", "644"],
  ["../installer/trained-model/generator.onnx", "usr/share/synth1gan/model/generator.onnx", "644"],
  ["../installer/trained-model/normalization.json", "usr/share/synth1gan/model/normalization.json", "644"],
]

[package.metadata.generate-rpm]
assets = [
  { source = "target/release/synth1gan", dest = "/usr/bin/synth1gan", mode = "755" },
  { source = "assets/synth1gan.desktop", dest = "/usr/share/applications/synth1gan.desktop", mode = "644" },
  { source = "../installer/trained-model/generator.onnx", dest = "/usr/share/synth1gan/model/generator.onnx", mode = "644" },
  { source = "../installer/trained-model/normalization.json", dest = "/usr/share/synth1gan/model/normalization.json", mode = "644" },
]
